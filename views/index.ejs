<%- include('./partials/html-head') %>
<link rel="stylesheet" href="/stylesheets/index.css" />
<%- include('./partials/nav') %>


<main>
  <h2> ☺︎ Hello  <%= user ? user.profile.name  : "fellow k-drama lover" %>☺︎ </h2>
  <% if (user?.profile.avatar) { %>
    <div>
      <img class="avatar"
        src="<%= user.profile.avatar %>"
        alt="<%= user.profile.name %> avatar"
      />
    </div>
  <% } %>
<h1> Oh My Drama</h1>
<p>An app where you can log,rate & track all the drama's you have <em>binged</em></p>
  <img src="assets/heart.png" alt="a hand heart" size="32x32">
</main>

<%- include('./partials/footer') %>
